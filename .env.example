# Copy to .env and fill in. .env is gitignored and must stay that way.
#
# Rule: this file holds fake values only. If a value here is ever real, it is
# already public — rotate it. Nothing in the repo is a secret store.
#
# Nothing is wired up yet, so this is deliberately near-empty. Variables get
# added here in the same commit that first reads them, never speculatively.

# TURN/STUN for premise B (P2P collaboration). Unused until the premise is
# decided at M5. The real static-auth-secret lives only on the coturn hosts.
# CLAVEN_TURN_URLS=turn:turn.example.org:3478,turns:turn.example.org:5349
# CLAVEN_TURN_SECRET=fake-not-a-real-secret
